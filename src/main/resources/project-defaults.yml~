swarm:
  datasources:
    jdbc-drivers:
      org.postgresql:
        driver-class-name: org.postgresql.Driver
        xa-datasource-class-name: org.postgresql.xa.PGXADataSource
        driver-module-name: org.postgresql
    data-sources:
      webhook:
        driver-name: org.postgresql
        connection-url: jdbc:postgresql://localhost:5432/machpay_webhook"
        user-name: postgres
        password: postgres